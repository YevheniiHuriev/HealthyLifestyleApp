import React from 'react'

/*
    getSpecialistType() - повертає спеціаліста, якого обрали в профілі (doctor, coach, psychologist, nutritionist)
    
    Реалізацію кнопки(повзунку) дивись в кінці профілю. 
    Відразу, як потрапляємо на сторінку встановлюємо setIsSpecialistProfile(checked); щоб кнопка була активна.
    При натисканні по аналогії.. Видаляємо поле "specialist-profile" з localStorage, перекидаємо на звичайний профіль.
*/

const SpecialistsProfilePage = () => {

    // const getSpecialistType = () => {
    //     return localStorage.getItem("specialist-profile");
    // };

    return(
        <>
        </>
    );
}

export default SpecialistsProfilePage;
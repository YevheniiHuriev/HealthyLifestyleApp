function WaterWidget({className}) {
    return (
        <div className={`${className} glass-card glass-card-water`}>
            <h1>Water Widget</h1>
        </div>
    )
}

export default WaterWidget;
function StepsWidget({ className }) {
    return (
        <div className={`${className} glass-card glass-card-steps`}>
            <h1>Steps Widget</h1>
        </div>
    )
}

export default StepsWidget;
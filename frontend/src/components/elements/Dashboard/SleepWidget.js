function SleepWidget({className}) {
    return (
        <div className={`${className} glass-card glass-card-sleep`}>
            <h1>Sleep Widget</h1>
        </div>
    )
}

export default SleepWidget;